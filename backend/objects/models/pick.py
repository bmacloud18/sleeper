class Pick:
    def __init__(self, draft_id, player_id, round_num, pick_num):
        self.draft_id = draft_id
        self.player_id = player_id
        self.round_num = round_num
        self.pick_num = pick_num